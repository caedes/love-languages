/**
 * Les trois couleurs du cœur en pixel art, indexées comme la grille de `heart.js` :
 * 0 = fond (le même bleu nuit que --color-bg et que la balise theme-color),
 * 1 = carmin, 2 = le liseré plus rose qui détache le cœur du fond.
 *
 * Partagée par le script d'icônes et par le logo de l'accueil : deux carmins
 * déclarés séparément dériveraient l'un de l'autre au premier changement.
 */
export const PALETTE = ['#161826', '#c51f3e', '#e8607f'];
